@model client.Models.HomeModel
@{
    ViewData["Title"] = "Home Page";
}

<link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />

<body>
    <div class="container-fluid ">
        <div class="w-25">
            <button class="btn btn-primary d-md-none m-2 w-25" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasExample"
                    aria-controls="offcanvasExample">
                HAM
            </button>
        </div>


        <div class="offcanvas offcanvas-start d-md-none bg-black" tabindex="-1" id="offcanvasExample" aria-labelledby="offcanvasExampleLabel">
            <div class="offcanvas-header bg-black">
                <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
            </div>
            <div class="offcanvas-body bg-black">

                <div class="col-sm sidebar-buttons">
                    <button class="sidebar-btn" id="chatBtn1">C</button>
                    <button class="sidebar-btn" id="fileBtn1">F</button>
                </div>
                <button class="btn" id="newChatBtnOffcanvas">
                    <img class="imgLogo" src="/img/Logo_full_no_nome.svg" alt="ㅤ">
                    Nuova chat
                </button>
                <div class="cronologia mt-3" id="chatSection1">
                    <div class="file">
                        <button>Saluto generale</button>
                    </div>
                    <div class="file">
                        <button>Saluto generale 2</button>
                    </div>
                </div>
                <div class="files hidden" id="fileSection1">
                    <div class="file" onclick="openFile('file1.pdf')">
                        <button>File 1</button>
                    </div>
                    <div class="file" onclick="openFile('file2.pdf')">
                        <button>File 2</button>
                    </div>
                    <div class="file" onclick="openFile('file3.pdf')">
                        <button>File 3</button>
                    </div>
                </div>
            </div>
        </div>

        <div class="row no-gutters">
            <!-- Sidebar -->
            <div class="col-md p-25 sidebar d-none d-md-block">
                <form method="post" asp-action="ChatPost">
                    <div class="newChat file text-center">
                        <button class="btn" id="newChatBtn">
                            <img class="imgLogo" src="/img/Logo_full_no_nome.svg" alt="ㅤ">
                            Nuova chat
                        </button>
                    </div>
                </form>
                <div class="cronologia mt-3" id="chatSection">
                    @if (Model.Chats != null)
                    {
                        foreach (var chat in Model.Chats)
                        {
                            if (Model.SelectedChat != null && Model.SelectedChat.Id == chat.Id)
                            {
                                <div class="file file-selected">
                                    <form asp-action="ChatGetById" method="get">
                                        <input type="hidden" name="id" value="@chat.Id" />
                                        <button class="btn btn-primary">@chat.Title</button>
                                    </form>
                                </div>
                            } else
                            {
                                <div class="file">
                                    <form asp-action="ChatGetById" method="get">
                                        <input type="hidden" name="id" value="@chat.Id" />
                                        <button class="btn btn-primary">@chat.Title</button>
                                    </form>
                                </div>
                            }
                        }
                    }
                </div>
                <div class="files hidden" id="fileSection">
                    @if (Model.Documents != null)
                    {
                        foreach (var file in Model.Documents)
                        {
                            <div class="file">
                                <button>@file.Name</button>
                            </div>
                        }
                    }
                </div>
                <div class="col-sm sidebar-buttons">
                    <button class="sidebar-btn" id="chatBtn">C</button>
                    <button class="sidebar-btn" id="fileBtn">F</button>
                </div>
            </div>
            <!---------------MESSAGGI CHAT--------->

            <div class="col-md-9 d-flex flex-column p-0">
                <main class="content flex-grow-1">
                    <div class="chat flex-grow-1 p-3">
                        @if (Model.SelectedChat != null && Model.SelectedChat.Messages != null)
                        {
                            for (int i = 0; i < Model.SelectedChat.Messages.Count; i++)
                            {
                                var message = Model.SelectedChat.Messages[i];
                                if (message.Role == client.Enum.ChatRole.USER) // User message
                                {
                                    <div class="message d-flex">
                                        <span>@message.Text</span>
                                    </div>
                                }
                                else // Bot message
                                {
                                    <div class="response d-flex">
                                        <span>
                                            @message.Text
                                            <div class="tag-group">
                                                @if (message.DocumentChunksUniqueNames != null)
                                                {
                                                    <div class="response">
                                                        @foreach (var chunkName in message.DocumentChunksUniqueNames)
                                                        {
                                                            <div class="tag">
                                                                @chunkName
                                                            </div>
                                                        }
                                                    </div>
                                                }
                                            </div>
                                        </span>
                                    </div>
                                }
                            }
                        }
                    </div>
                </main>
            <div/>
                <footer>
                    <div class="input-container">
                        <button id="attachButton" class="attach-button" type="button" aria-haspopup="menu" aria-expanded="false" data-state="closed" data-bs-toggle="modal" data-bs-target="#fileModal">
                            📎
                        </button>
                        <form asp-action="MessagePost" method="post">
                            <input type="text" placeholder="Invia un messaggio" class="input-field" asp-for=Message/>
                            <button class="send-button">↥</button>
                        </form>
                    </div>
                </footer>
            </div>
        </div>
    </div>

   @*  POPUP PER APRIRE ESPLORA FILE *@
    <div class="modal fade" id="fileModal" tabindex="-1" aria-labelledby="fileModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="fileModalLabel">Seleziona File e scrivi i tags</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <form enctype="multipart/form-data" method="post" asp-action="DocumentPost">
                    <div class="modal-body">
                        <input asp-for="UploadDocument.FormFiles" type="file" class="form-control mb-3" multiple>
                        <input asp-for="UploadDocument.MetaData" type="text" placeholder="Scrivi i tags" class="form-control">
                    </div>
                    <div class="modal-footer">
                        <button type="submit" class="btn btn-primary" id="modalSubmitButton">Invia</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    @* <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script> *@
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM"
            crossorigin="anonymous"></script>
    <script src="~/js/site.js"></script>
</body>

@* CODICE VECCHIO DI FOFO *@
@* <body>
    <div class="container">
        <aside class="sidebar">
            <div class="logo">
                <h2>ChatGPT</h2>
            </div>
            <div class="cronologia">
                <div class="file">
                    <button>Presentazione</button>
                </div>
                <div class="file">
                    <button>Appunti scuola</button>
                </div>
                <div class="file">
                    <button>aaaaaaaaaaaa</button>
                </div>
            </div>
        </aside>
        <div class="sidebar-buttons">
            <button class="sidebar-btn" id="chatBtn">C</button>
            <button class="sidebar-btn" id="fileBtn">F</button>
        </div>
        <main class="content">
            <div class="chat" id="chatSection">
                <div class="message">
                    <span>Ciao, come va?</span>
                </div>
                <div class="response">
                    <span>Va tutto bene, grazie. E tu, come stai? C'è qualcosa in particolare di cui vuoi parlare o su cui hai bisogno di aiuto?</span>
                </div>
            </div>
            <div class="files" id="fileSection" style="display: none;">
                <div class="file" onclick="openFile('file1.pdf')">
                    <button>File 1</button>
                </div>
                <div class="file" onclick="openFile('file2.pdf')">
                    <button>File 2</button>
                </div>
                <div class="file" onclick="openFile('file3.pdf')">
                    <button>File 3</button>
                </div>
            </div>
            <footer>
                <form class="attach-button" runat="server">
                    <div>
                        <asp:FileUpload ID="FileUpload1" runat="server" AllowMultiple="true" />
                        <asp:Button ID="UploadButton" runat="server" Text="Upload Files" OnClick="UploadButton_Click" />
                        <asp:Label ID="StatusLabel" runat="server" Text="" />
                    </div>
                </form>
                <input type="text" placeholder="Invia un messaggio">
                <button class="send-button">↥</button>
            </footer>
        </main>
    </div>
    <script src="script.js"></script>
</body> *@
